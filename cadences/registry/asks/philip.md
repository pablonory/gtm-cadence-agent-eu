# Ask — Phil (philip@nory.ai)

Paste into Slack. Regenerated by `scripts/gong_flows.py`; replies go into `cadences/registry/philip.json` (`live: true/false`).

---

Hi Phil — two minutes for the outbound agent, if you have them.

I can't see any reusable templates under your name in Gong — which probably means you run the shared flows, or yours live in a shared folder.

Two more things:

- If you run something that isn't on the list, tell me the name exactly as it appears in Gong.
- Do you use the shared **Outbound** flows (Bar/Pub · Coffee · Resto × CEO / Finance / IT / Ops / People, plus the SMB set)? If so, which ones?

**Why I'm asking:** when the agent hands you an account brief it will name the Gong flow to drop that account into. I want it naming *your* flows, not a generic set — and I'd rather ask you than guess from flow names.

---

*Note for Pablo: Phil also sees 79 `Shared` flows. Gong does not report who created a flow, so any of those could be theirs — worth a follow-up if the answer above is thin.*
