# Slack announcement — the agent, to the UKI reps (Pablo sends)

Goes out BEFORE the per-rep flow asks (`asks/<rep>.txt`), so the DM lands with context already given.
Pablo's voice, British English. Two Loom links and the HubSpot list link to be added by Pablo where marked.

Channel: no UKI-specific channel exists — `#sales-crew` (private) or `#sales` (public, mixed with US).
Audience: 10 AEs + 3 BDRs (`../uki_reps.json`).

Updated 2026-09-15: added the sample ask (each rep sends a Google Sheet with 15–20 accounts)
and the DM heads-up with the "cadences not sorted" escape hatch. Previous version at git history.

---

:noryheart: Building an agent that preps your outbound accounts - here's what it does and the two things I need from you.

Two Looms, they explain it better than I can here:
[LOOM 1 - what the agent does]
[LOOM 2 - what a brief looks like in HubSpot]

What you get, per account:
• why now - new sites, funding, a new ops or finance hire, open roles, each with the source link
• a first touch written for that account, plus a softer version of it
• the Gong flow to drop them into

It writes into HubSpot and stops there. Nothing sends, nothing gets enrolled - you run the cadence in Gong exactly like you do today.

The two things:

1. Pick 15-20 accounts you want it to run on first and send me a Google Sheet with them - company name and website is enough. That's the first batch - you'll get briefs on your own accounts, not a demo set.

2. You'll get a Slack DM from me this week with a short survey about your Gong flows. The agent builds on YOUR cadences, so it needs to know which ones you actually run. And if your flows are a bit of a mess or you don't really have a set - say so in the survey, that's exactly what it's for. The agent will help you build better ones.

And something you can use today - https://uki-market-signals.netlify.app - what's true in the market right now [April wage step, the Irish VAT cut, business rates, food costs], quantified per site. For when there's nothing account-specific to open with. :nory-rocket:
