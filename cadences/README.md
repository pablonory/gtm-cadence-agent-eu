# Cadences — map to the Gong UKI flows (not agent-designed)

**Source of truth: [`UKI_FLOWS.md`](UKI_FLOWS.md) — currently a ⚠️ PLACEHOLDER.** The Gong flows UKI
reps use are being investigated (Pablo, 2026-08-20); until the real folder + flow names are captured
in that file, the agent **maps nothing**: briefs carry an empty `cadence_template` and a
*"flow pending"* note. The agent does **not** design cadences — same doctrine as the US fork.
Per account, the agent still fills the one bespoke slot (first-touch email, Output B).

## The matrix — inherited candidate (4 × 4 + reactivation), NOT confirmed for UKI

Suite derives from persona: **C-Suite & Founder → Full Suite**; **Finance & Operations → IM**.
Candidate structure + the capture checklist live in [`UKI_FLOWS.md`](UKI_FLOWS.md). The UKI-specific
open question: **pubs & bars** (no US cell; gastropubs currently classify FSR).

## The per-cell `*.md` files are angle/proof briefs — not cadence designs

Inherited from the US fork: `coffee_cafe_finance.md`, `fsr_finance.md`, … carry **persona brief ·
pain × JTBD angle · proof · VOC** feeding the first touch. Their day-by-day flow tables are
historical. Bonus for this fork: their proof points are largely **UKI-native £/€ results** —
quotable directly here, no currency translation rule needed (see `knowledge/proof_library.md`).

**Coverage gap (inherited):** the 9 files cover 3 verticals × {Ops, Finance, C-suite}. Fast Casual ×4
and Founder ×4 briefs were never written; a pubs & bars set may be needed too, pending the vertical
decision. None of this blocks Stage 1/2 — it gates first-touch *quality* on those cells only.

`_flow_structure.md` (the old default sequence) is retained for reference only.
