# Open questions — every unknown, with an owner and the file its answer unblocks

> Doctrine rule (`agent-doctrine` §5): an unknown without an owner is a stall, not a question.
> Statuses: 🔴 blocks the first real batch · 🟡 blocks a later stage · ✅ closed (kept for the record).

| # | Status | Question | Owner | Answer lands in |
|---|---|---|---|---|
| 1 | ✅ | ~~Who owns UKI territory + flows?~~ — **Pablo is interim owner** (decided 2026-09-02) until a sales-side owner is named; flow knowledge comes from each rep, not a manager (#2) | — | `CLAUDE.md` header, `input/README.md` |
| 2 | 🟡 | **Which Gong flows do UKI reps actually use?** STRUCTURE captured 2026-09-11 (`scripts/gong_flows.py`): 73-flow company layer + each rep's own. STILL OPEN: which are **live**. No shortcut exists — per-flow usage is absent from the Gong API (verified) and flow choice is each rep's own, so there is no manager who knows. Asks are generated and ready to send | **each rep** (13 × 2 min); Pablo sends `cadences/registry/asks/<rep>.txt` | `cadences/registry/<rep>.json` |
| 3 | 🔴 | **UKI accounts sheet** — create to schema, WITH data-validation dropdowns (US template's lack of them is a recorded defect); validate rep emails as HubSpot owners | **Pablo** (as interim owner) | `input/README.md`, the sheet itself |
| 4 | 🟡 | **Pubs & bars** — own vertical or fold into FSR? Evidence now in hand: the Gong company layer has a **Bar/Pub segment** (5 flows) and reps run pub flows (Sean, Edward, Louis G); the CRM enum has `Pub`. **Recommendation: add `pubs_bars` as a 5th vertical.** Needs: sheet dropdown, `verticals.md`, pains/proof (Hampshire Pub Co., Shepherd Neame evidence exists), scorer/mapper enums | **Pablo** (decision) | `cadences/UKI_FLOWS.md`, `context/icp/verticals.md`, `hubspot-app/scripts/*.py` |
| 5 | 🟡 | **Stage 2b for UKI** — pull UKI calls from the shared Gong instance (filter by UKI reps/deals) and build the evidence packs; blocked on #1 for the rep list | Pablo | `directives/stage2b_gong_evidence.md`, `knowledge/gong_evidence/` |
| 6 | ✅ | ~~UKI recall re-measurement~~ — **measured 2026-09-17**: William's 15-account batch 1, 60 hunter dispatches, all first runs. `new_location` 6/15 real vs 2/15 fired (dead zone at 204–346d, same shape as US 3/10); `leadership_hire` 0/15 fired but Companies House gives high-confidence dated negatives, a clear method win over the US signal; `open_jobs` 0/15 ATS boards found, probe-first confirmed at a different account-size mix; `funding` 0/15, four namesake traps + one lorem-ipsum block rejected. Full detail, incl. the four hand-overrides that feed #4: `directives/signals/_signal_stack.md` | — | `directives/signals/_signal_stack.md` |
| 7 | 🟡 | **UKI ATS probe gap** — Harri/Fourth/S4labour/Flow have no public JSON API in `jobs_probe.py`; decide whether to build fingerprints for them after the first batch measures the miss rate | Pablo, after #6 | `scripts/jobs_probe.py`, `directives/signals/open_jobs.md` |
| 8 | ✅ | ~~UK conjunctural register~~ — built 2026-08-20 from primary sources, 16 UK+IE entries; matcher UKI-adapted (`ea0f161`). Watch refresh dates: first expiries Sept 2026 | — | `knowledge/conjunctural/README.md` |
| 9 | ✅ | ~~Ireland € proof~~ — Masa is €-native; IE wage/law register entries built 2026-08-20 | — | `knowledge/proof_library.md`, `knowledge/conjunctural/register/ireland.json` |
| 10 | 🟡 | **IT and People personas** — the Gong company layer sells to IT and People/HR (10 flows + personal ones); the agent classifies only C-Suite/Finance/Founder/Operations, so contacts with those titles land as *unmapped*. **First rep-backed answer, 2026-09-18 (Magnus, the ENT AE and the only rep running an IT-persona flow): "We should target People/HR and IT as well as the normal ICP. e.g payroll and compliance assistants match well with HR persona."** He supplies the mechanism too — the persona is reached through the *module* (payroll/compliance → HR), which ties this to #13. His book carries the evidence: IT contacts plus "head of people" among unmapped titles. Decision now needs JTBD/pains for each, not more evidence | **Pablo** — evidence is in | `context/icp/personas.md`, `map_contacts.py`, first-touch skill |
| 13 | 🟡 | **Module as a routing dimension** — the agent derives only **IM vs Full Suite**, from persona. Magnus (2026-09-18) routes on the module itself and wants it signal-driven: *"We should route WF, IM, Full Suite, Payroll and even Reviews… if we have the signals that a particular module might solve their problem. Then we can widen the scope in process. OR we route via the Assistants — Forecasting Assistant, Scheduling assistant etc."* His own flow names already encode it (`Coffee: Labour`, `QSR: Labour`, `AE FLOW - Labour Assessment/AgenticAI Schedules`). Needs: the module list settled, a signal→module mapping, and benefits/proof per module. Ties to #10 (payroll/compliance → HR) | **Pablo** (scope), then the agent | `context/icp/`, `knowledge/benefits.md`, `score_accounts.py`, first-touch skill |
| 14 | 🟡 | **Franchise as its own motion** — 6 of the 8 accounts Magnus offered are franchise groups (Yum!, GDK, 3× Costa franchisees, BWG) and he has run a `Domino's Franchise Owners` flow since April. None of the four verticals describes a franchisee, and he says the **angle** differs, not just the segment: *"Franchise owners care about scaling their site numbers… We don't directly increase revenue, we do so indirectly by increasing profit and making it viable to take on new sites."* i.e. growth-enablement, not cost-saving. Needs: a franchise flag (orthogonal to vertical, not a fifth value) and its own benefit framing | **Pablo** (decision), evidence from batch 2 | `context/icp/verticals.md`, `knowledge/benefits.md`, first-touch skill |
| 11 | ✅ | ~~Rewire the matcher from build to lookup~~ — **built 2026-09-17**: `lib/flow_registry.py` resolves flows from the rep's registry (own → company → EMPTY + note), tri-state `live` → `flow_status: confirmed|suggested`, reason-based reactivation, US folders structurally excluded. `score_accounts.py`, `map_contacts.py` and `upsert_brief.py` rewired; 13 new tests (50 total green). Verified end-to-end on William's confirmed registry | — | done — `lib/flow_registry.py`, `tests/test_flow_registry.py` |
| 12 | ✅ | ~~Cross-repo: does the per-rep cadence model change the US agent?~~ — **No** (Pablo, 2026-09-15). The per-rep work is **UKI-only**: `gtm-cadence-agent` keeps Lewis's shared 16-flow matrix and its build-from-pattern matcher, unchanged. #11 is therefore a UKI rewiring, not a shared one — build it for this repo and do not wait on, or touch, the US side | — | this repo's #11 only |
